package com.thont.user.zother.listener;


import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.event.EventListener;
import org.springframework.stereotype.Component;

@Component
public class Listener {
//    @Autowired
//    private MailBusiness mailBusiness;
//    @Autowired
//    private SmsService smsService;
//
//    @EventListener(UserRegisterEvent.class)
//    public void sendConfirmEmail(UserRegisterEvent event) {
//        smsService.sendOTP(((Parent) event.getUser()).getPhone(), event.getOtp().getOtp());
////       mailBusiness.sendAccountRegisterConfirm(event);
//    }
}
